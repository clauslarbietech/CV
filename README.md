# HERO — Inclusive reading platform

The HERO web MVP currently lives in `lexrise/` (package name: **hero**).

```bash
cd lexrise && npm install && npm run dev
```

Start at `/splash` for onboarding.

**Preview (temporary):** https://clauslarbietech.github.io/CV/lexrise/

## Dedicated home (in progress)

Target repository: https://github.com/clauslarbietech/Dyslexia-support-app

A full copy (source + GitHub Pages workflow + `/Dyslexia-support-app` basePath) is prepared for that repo. Push is blocked until this Cloud Agent has write access to `Dyslexia-support-app`. See `lexrise/TRANSFER.md`.

HERO supports reading and learning—it does not diagnose dyslexia.
